/*
Task Description - Write a Javascript function that takes a number's
Decimal representation as input, and returns an array of the same
number in the Javascript representation of 64 bits.
*/

/** Returns the Javascript representation of a binary
 * @param {Number} decimalRepresentation - Decimal representation of the number
 * @returns {Array<Number>} - 64 bits Javascript representation
 */
function getJSNumberRepresentation(decimalRepresentation) {
    const signBit = (decimalRepresentation < 0 ? 1 : 0)
    
}